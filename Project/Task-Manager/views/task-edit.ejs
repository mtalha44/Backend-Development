<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Update Task | Task Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body class="bg-gray-800">
  <div class="min-h-screen flex flex-col items-center justify-center px-4">
    
    <!-- Back Navigation -->
    <div class="mb-4 self-start">
      <a href="/task/app/create/<%= userId %>" class="text-blue-500 hover:text-blue-400">&larr; Back</a>
    </div>
    
    <!-- Existing Task Display -->
    <div class="bg-zinc-700 rounded-lg shadow p-4 w-full max-w-md mb-6">
      <h2 class="text-xl font-bold text-white mb-2"><%= prevtopic.topic %></h2>
      <p class="text-gray-300 break-words"><%= prevtopic.description %></p>
      <div class="mt-3 text-right">
        <a href="/task/app/delete/<%= prevtopic._id %>/<%= userId %>" class="text-red-500 hover:text-red-400 font-medium">
          Delete Task
        </a>
      </div>
    </div>
    
    <!-- Update Task Form -->
    <div class="bg-white rounded-lg shadow p-4 w-full max-w-md">
      <h1 class="text-center text-lg font-bold text-gray-800 mb-4">Update Task</h1>
      <form action="/task/app/update/<%= prevtopic._id %>/<%= userId %>" method="post" class="space-y-4">
        <div>
          <label for="newtopic" class="block text-gray-700 text-sm mb-1">New Topic</label>
          <input type="text" name="newtopic" id="newtopic" placeholder="Enter new topic" 
                 class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-400" />
        </div>
        <div>
          <label for="newdescription" class="block text-gray-700 text-sm mb-1">New Description</label>
          <textarea name="newdescription" id="newdescription" rows="3" placeholder="Enter new description" 
                    class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-400"></textarea>
        </div>
        <button type="submit" 
                class="w-full py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition">
          Update Task
        </button>
      </form>
    </div>
    
  </div>
</body>
</html>
